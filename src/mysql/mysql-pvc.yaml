apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  storageClassName: standart
  volumeName: mysql-pv
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi